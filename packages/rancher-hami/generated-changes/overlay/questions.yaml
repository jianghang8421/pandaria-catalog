questions:
# hami-webui configurations
- variable: hami-webui.enabled
  default: true
  description: "Enable HAMi WebUI"
  type: boolean
  label: Enable HAMi WebUI
  group: "HAMi WebUI"
  show_subquestion_if: true
  subquestions:
  - variable: hami-webui.externalPrometheus.enabled
    default: true
    description: "Use external prometheus"
    type: boolean
    label: Use External Prometheus
  - variable: hami-webui.externalPrometheus.address
    default: "http://rancher-monitoring-prometheus.cattle-monitoring-system.svc.cluster.local:9090"
    description: "External Prometheus Address. Default is Rancher Monitoring prometheus address."
    type: string
    label: External Prometheus Address

# scheduler configurations
- variable: defaultSchedulerPolicy.nodeSchedulerPolicy
  description: "Default value is binpack, representing the GPU node scheduling policy. binpack means trying to allocate tasks to the same GPU node as much as possible, while spread means trying to allocate tasks to different GPU nodes as much as possible."
  type: enum
  default: "binpack"
  options:
    - binpack
    - spread
  label: Node Scheduler Policy
  group: "Scheduler Config"
- variable: defaultSchedulerPolicy.gpuSchedulerPolicy
  description: "Default value is spread, representing the GPU scheduling policy. binpack means trying to allocate tasks to the same GPU as much as possible, while spread means trying to allocate tasks to different GPUs as much as possible."
  type: enum
  default: "spread"
  options:
    - binpack
    - spread
  label: GPU Scheduler Policy
  group: "Scheduler Config"

# device plugin configurations
- variable: devicePlugin.runtimeClassName
  type: string
  label: RuntimeClassName
  group: "Device Plugin Config"
- variable: devicePlugin.migStrategy
  description: '"none" for ignoring MIG features or "mixed" for allocating MIG device by seperate resources. Default "none"'
  type: enum
  default: "none"
  options:
    - none
    - mixed
  label: MIG Strategy
  group: "Device Plugin Config"


# nvidia configurations
- variable: resourceName
  description: "NVIDIA Resource Name"
  type: string
  label: NVIDIA Resource Name
  group: "Resource Name"
- variable: resourceMem
  description: "NVIDIA Memory Resource Name"
  type: string
  label: NVIDIA Memory Resource Name
  group: "Resource Name"
- variable: resourceMemPercentage
  description: "NVIDIA Memory Percentage Resource Name"
  type: string
  label: NVIDIA Memory Percentage Resource Name
  group: "Resource Name"
- variable: resourceCores
  description: "NVIDIA Cores Resource Name"
  type: string
  label: NVIDIA Cores Resource Name
  group: "Resource Name"
- variable: resourcePriority
  description: "NVIDIA Priority Resource Name"
  type: string
  label: NVIDIA Priority Resource Name
  group: "Resource Name"
